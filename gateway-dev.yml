server:
  port: 6001

spring:
  application:
     name: gateway-zuul
  sleuth:
    web:
      client:
        enabled: true
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9411/
